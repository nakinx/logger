
language: c++
compiler:
  - gcc

logger:
  include:    
    - os: linux
      addons:
        apt:
          sources:
            - ubuntu-toolchain-r-test
          packages:
            - g++-7

      env:
        - LOGGER_EVAL="CC=gcc-7 && CXX=g++-7"

before_install:
  - eval "${LOGGER_EVAL}"

script: make
